% NOTE: Parts of the file are withheld but available on request.
% Structure, documentation, and descriptions are intact.  the file will
% not run as posted.

function [UEXACT] = exact(NX, NY, D1, D2, ALPHA, BETA, t)
    % This function creates a grid of the correct size and then populates
    % the grid with the correct solution.  The correct solution is a
    % function of x, y, and t

    % Inputs:
    %   NX = number of points in the x direction
    %   NY = number of points in the y direction
    %   D1 = first diffusivities for the x direction
    %   D2 = second diffusivities for the y direction
    %   ALPHA = integer mode number for the IC
    %   BETA = integer mode number for the IC
    %   t = time of the exact solution
    % Outputs:
    %   UEXACT = the solution grid populated with the correct answer

    % REDACTED: exact solution code is available on request
end